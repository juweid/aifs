[project]
name = "aifs"
version = "0.1.0"
description = "UV Project containing multiple ways to run the AIFS weather forecast model"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "zarr>=3.4.0",
]

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
